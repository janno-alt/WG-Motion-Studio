// Loaded at build time from the remotion/presets directory so both preview
// and final rendering share a single source of truth.
import planningPrompt from "@remotion-project/presets/planning-prompt.md?raw";

export const PLANNING_PROMPT = planningPrompt;
